<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Booking List</title>

  <style>
    * {
      box-sizing: border-box;
      font-family: "Segoe UI", Tahoma, sans-serif;
    }

    body {
      margin: 0;
      padding: 40px;
      background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
      min-height: 100vh;
    }

    .container {
      max-width: 800px;
      margin: auto;
      background: #ffffff;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    }

    h2 {
      margin-bottom: 25px;
      text-align: center;
      color: #333;
      letter-spacing: 1px;
    }

    ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    li {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 20px;
      margin-bottom: 12px;
      border-radius: 8px;
      background: #f9fafb;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    li:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.08);
    }

    .info {
      display: flex;
      flex-direction: column;
      gap: 5px;
    }

    .customer {
      font-weight: 600;
      color: #2c3e50;
    }

    .car {
      font-size: 14px;
      color: #555;
    }

    .price {
      font-weight: bold;
      color: #27ae60;
      font-size: 16px;
    }

    .empty {
      text-align: center;
      color: #888;
      font-style: italic;
    }
  </style>
</head>

<body>
  <div class="container">
    <h2>Danh sách Booking</h2>

    <% if (bookings.length === 0) { %>
      <p class="empty">Chưa có booking nào</p>
    <% } else { %>
      <ul>
        <% bookings.forEach(b => { %>
          <li>
            <div class="info">
              <span class="customer"><%= b.customerName %></span>
              <span class="car">Xe: <%= b.carNumber %></span>
            </div>
            <span class="price">
              <%= b.totalPrice.toLocaleString("vi-VN") %> đ
            </span>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </div>
</body>
</html>
